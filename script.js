const $=s=>document.querySelector(s);
function icon(c){return ({Disney:"🏰",Universal:"🎢",Travel:"✈️",Fitness:"💪",Faith:"🙏",Pets:"🐾"})[c]||"✨"}
function card(p){return `<article class="post-card"><div class="post-art">${icon(p.category)}</div><div class="post-body"><div class="tag">${p.category}</div><h3>${p.title}</h3><p>${p.excerpt||""}</p><a class="read" href="${p.slug}.html">Read post →</a></div></article>`}
function render(){const latest=POSTS.slice(0,6);$("#latest-posts").innerHTML=latest.map(card).join(""); const map={Disney:"disney-posts", Universal:"universal-posts", Travel: "travel-posts", Fitness: "fitness-posts", Faith: "faith-posts", Pets: "pets-posts"}; for(const [c, id] of Object. entries(map)){const el=$("#"+id); if(el)el.innerHTML=POSTS.filter(p=>p.category===c).map(card).join("")||"<p>New adventures coming soon.</p>"}}
document.addEventListener("DOMContentLoaded",()=>{render();document.getElementById("year").textContent=new Date().getFullYear();const b=document.querySelector(".hamb"),n=document.querySelector("nav");if(b)b.addEventListener("click",()=>n.classList.toggle("open"))});
