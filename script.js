function card(p){return `<article class="post"><div class="art">${p.icon}</div><div class="body"><div class="tag">${p.cat}${p.new?' · NEW':''}</div><h3>${p.title}</h3><p>${p.text}</p><a class="read" href="#home" onclick="showPost('${p.id}');return false">Read post →</a></div></article>`}
function render(){document.querySelector('#latest-posts').innerHTML=POSTS.slice(0,6).map(card).join('');['Disney','Universal','Travel','Fitness','Faith','Pets'].forEach(c=>{let id='#'+c.toLowerCase()+'-posts';let el=document.querySelector(id);if(el)el.innerHTML=POSTS.filter(p=>p.cat===c).map(card).join('')||'<p>New adventures coming soon.</p>'})}
function showPost(id){const p=POSTS.find(x=>x.id===id);if(p)newModal(p)}function newModal(p){let old=document.querySelector('.modal');if(old)old.remove();let m=document.createElement('div');m.className='modal';m.innerHTML=`<div class="modal-box"><button onclick="this.closest('.modal').remove()">×</button><div class="tag">${p.cat}</div><h2>${p.title}</h2><p>${p.text}</p><p><strong>This new site is ready for your full article.</strong> The existing Blogger post can be pasted/imported here when we migrate the original content.</p></div>`;document.body.appendChild(m)}
document.querySelector('.hamb').onclick=()=>document.querySelector('nav').classList.toggle('open');document.querySelector('#year').textContent=new Date().getFullYear();render();
